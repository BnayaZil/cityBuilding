{
  "name": "@city-building/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "vitest run",
    "verify:3d": "vitest run tests/scene-layout.spec.ts",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@city-building/renderer": "0.1.0",
    "@city-building/shared": "0.1.0"
  }
}
